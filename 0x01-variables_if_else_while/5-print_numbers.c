#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry level
 *
 * Descriptiom: 'Program to print single digits'
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	char a;

	while (c <= '9')
	{
		putchar(c);
		c++;
	}
	putchar('\n');
	return (0);
}
